---
// Copyright (c) Pascal Brand
// MIT License

type Props = { name: string, class: string}

---

<create-leaflet-divicon data-props={JSON.stringify(Astro.props)} />

<script>
  import L from "leaflet"
  import { useIcon } from '../index'

  class CreateLeafletDivIcon extends HTMLElement {
    constructor() {
      super()
      const props = JSON.parse(this.dataset.props!)
      useIcon[props.name] = L.divIcon({className: props.class});
    }
  }

  customElements.get('create-leaflet-divicon') || customElements.define("create-leaflet-divicon", CreateLeafletDivIcon);
</script>
