# Map of github stars and google analytics country from

```
npm run dev
```

# Getting data

To get access to github API and google analytics API, `.env` file must be created
by copying `.env-example`, and then updating the tokens

```
npm run get-data       # get visited towns, countries, and stargazers countries
npm rnu get-candidates # get github candidates to use astro-swiper,...
```
